# ─────────────── Python ───────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ─────────────── 環境 / 套件 / 模型 ───────────────
# Conda env（10GB+），絕對不上 git
Miniforge3/
miniconda3/
venv/
.venv/
env/

# 下載的 AI 模型（>3GB）
models/

# 翻譯中間輸出 / 跑出來的圖片（4GB+）
result/
result-*/
*-translated/
*_translated/
manga_translator_work/

# ─────────────── 打包輸出 ───────────────
build/
dist/
*.spec.bak

# ─────────────── 系統 / 編輯器 ───────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ─────────────── catclaw 個人記憶（不上 git）───────────────
.claude/memory/personal/
.claude/memory/_staging/
.claude/memory/episodic/

# ─────────────── 環境變數 / API key ───────────────
.env
.env.local
*.access.json

# ─────────────── 個人 runtime 設定（UI/CLI 啟動自動產生，非範本）───────────────
# 範本見 examples/config-example.json；以下三檔每次跑都會被覆寫
examples/config.json
examples/custom_api_params.json
examples/filter_list.json

# ─────────────── 既有 .bat 殘留（macOS 不需）───────────────
# 已手動刪除，保留 ignore 以防之後 git pull 拉回又刪除衝突
